type RuleNode {
    useRegex: Boolean
    term:   String
    regex:  String
    max:    Int
    min:    Int
    and:    [RuleNode]
    or:     [RuleNode]
    none:   [RuleNode]
}
type RuleRoot {
    and:    [RuleNode]
    none:   [RuleNode]
}
type BucketConfRev {
    rule: RuleRoot
}
type BucketConf {
    revIpRules: BucketConfRev
}
type Bucket {
    name: String
    conf: BucketConf
}

# This is required by buildASTSchema
type Query { anything: ID }

